# Backend environment variables (NO subas .env a GitHub)

# Server
PORT=3000

# CORS (opcional) - si publicas el frontend en un dominio, ponlo aquí
# CORS_ORIGIN=https://tudominio.com

# Firebase Storage bucket
FIREBASE_STORAGE_BUCKET=mauiviajes-9e5a1.firebasestorage.app

# Firebase Admin credentials (elige UNA opción):
# Opción A: ruta al JSON fuera del repo (recomendado en local)
# FIREBASE_SERVICE_ACCOUNT_PATH=/ruta/segura/serviceAccount.json
#
# Opción B: JSON en una sola línea (útil para deploy con secretos)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Gmail (para alertas de leads)
GMAIL_USER=tu_correo@gmail.com
GMAIL_APP_PASSWORD=tu_app_password
LEADS_NOTIFY_TO=tu_correo@gmail.com
